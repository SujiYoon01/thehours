const rows = byTeam[t].map(r=>`${r['이름']}(${r['직급']})`).join('<br>');


const rows = byTeam[t].map(r=>`${r['이름']}(${r['직급']})`).join('<br>');


<div class="topbar">
    <h1>🛰️ 우주사업총괄 근무실적 현황</h1>
    <div class="topbar-actions">
